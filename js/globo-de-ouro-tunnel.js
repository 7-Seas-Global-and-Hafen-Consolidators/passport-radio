/* PASSPORT RADIO · GLOBO DE OURO TUNNEL™
   Isolated audio-only engine. No video/iframe is rendered.
   External stream endpoint is configurable; existing tunnel engines stay untouched.
*/
(()=>{"use strict";
const PANEL_ID="passportGloboOuro",AUDIO_ID="passportGloboOuroAudio",PLAY_ID="passportGloboOuroPlay",STATUS_ID="passportGloboOuroStatus";
const DEFAULT_STREAM="";
function streamUrl(){return String(window.PASSPORT_GLOBO_DE_OURO_STREAM||DEFAULT_STREAM).trim()}
function ensureDirectory(){const hub=document.getElementById("passportTunnels"),d=hub&&hub.querySelector(".tunnel-directory");if(!d||d.querySelector(`[data-tunnel-target="${PANEL_ID}"]`))return;const b=document.createElement("button");b.className="tunnel-directory__row";b.type="button";b.dataset.tunnelTarget=PANEL_ID;b.setAttribute("aria-controls",PANEL_ID);b.setAttribute("aria-expanded","false");b.innerHTML='<span class="tunnel-directory__number">08</span><strong class="tunnel-directory__title">Globo de Ouro Tunnel™</strong><span class="tunnel-directory__format">1973–1990 · apresentadores · plateia · performances · áudio</span><span class="tunnel-directory__state">24 HOURS</span><span class="tunnel-directory__action">Abrir player</span>';b.addEventListener("click",()=>{const p=document.getElementById(PANEL_ID);if(!p)return;const open=p.hidden;p.hidden=!open;p.setAttribute("aria-hidden",open?"false":"true");b.setAttribute("aria-expanded",open?"true":"false");if(open){document.querySelectorAll("[data-passport-tunnel-panel]").forEach(x=>{if(x!==p)x.hidden=true});p.scrollIntoView({behavior:"smooth",block:"nearest"})}else{const a=document.getElementById(AUDIO_ID);if(a&&!a.paused)a.pause()}});d.appendChild(b);const intro=hub.querySelector(".tunnels-intro p");if(intro)intro.textContent="Oito ambientes musicais contínuos, organizados sem transformar a página numa parede de players. Abra um por vez; a engenharia dos canais permanece independente."}
function ensurePanel(){const hub=document.getElementById("passportTunnels");if(!hub||document.getElementById(PANEL_ID))return;const panel=document.createElement("section");panel.id=PANEL_ID;panel.className="tunnel-panel passport-globo-ouro";panel.dataset.passportTunnelPanel="1";panel.hidden=true;panel.setAttribute("aria-hidden","true");panel.innerHTML=`<div class="live-shell"><div class="tunnel-panel__head"><span class="live-kicker">PASSPORT ARCHIVE SIGNAL™ · 1973–1990</span><h2>Globo de Ouro Tunnel™</h2><p>Somente áudio. Apresentadores, plateia e performances preservados em blocos contínuos de memória musical brasileira.</p></div><div class="tunnel-player"><button id="${PLAY_ID}" type="button" aria-label="Reproduzir Globo de Ouro Tunnel">▶</button><div><strong>GLOBO DE OURO TUNNEL™</strong><span>1973–1990 · AUDIO ARCHIVE · 24 HOURS</span></div><span id="${STATUS_ID}">READY</span></div><audio id="${AUDIO_ID}" preload="none"></audio></div>`;hub.appendChild(panel);const audio=document.getElementById(AUDIO_ID),play=document.getElementById(PLAY_ID),status=document.getElementById(STATUS_ID);const sync=()=>{play.textContent=audio.paused?"▶":"Ⅱ";status.textContent=audio.paused?"READY":"ON AIR"};play.addEventListener("click",async()=>{if(!audio.paused){audio.pause();sync();return}const src=streamUrl();if(!src){status.textContent="STREAM PENDING";return}if(audio.src!==src)audio.src=src;document.querySelectorAll("audio").forEach(a=>{if(a!==audio&&!a.paused)try{a.pause()}catch(_){}});try{await audio.play();sync()}catch(e){console.error("[Globo de Ouro Tunnel]",e);status.textContent="RETRY"}});audio.addEventListener("play",sync);audio.addEventListener("pause",sync);audio.addEventListener("error",()=>status.textContent="OFFLINE")}
function boot(){ensurePanel();ensureDirectory()}
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",boot,{once:true});else boot();
window.PassportGloboOuroTunnel={setStream:url=>{window.PASSPORT_GLOBO_DE_OURO_STREAM=String(url||"");const a=document.getElementById(AUDIO_ID);if(a){a.pause();a.removeAttribute("src");a.load()}},stop:()=>{const a=document.getElementById(AUDIO_ID);if(a&&!a.paused)a.pause()}};
})();
