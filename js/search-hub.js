(()=>{const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const cfg=window.PASSPORT_SEARCH_HUB||{};const root=$('#passport-search-hub');if(!root)return;fetch('/data/editorial-feed.json',{cache:'no-store'}).then(r=>r.ok?r.json():Promise.reject()).then(data=>{const items=Array.isArray(data.items)?data.items:[];const terms=(cfg.terms||[]).map(x=>String(x).toLowerCase());const match=i=>{const hay=[i.category,i.format,i.title,...(i.entities||[])].join(' ').toLowerCase();return !terms.length||terms.some(t=>hay.includes(t));};const selected=items.filter(match).sort((a,b)=>String(b.published_at||'').localeCompare(String(a.published_at||''))).slice(0,cfg.limit||60);root.innerHTML=selected.length?selected.map(i=>`<article><a href="${esc(i.url)}"><small>${esc((i.category||'música').replaceAll('_',' '))}</small><h2>${esc(i.title)}</h2><p>${esc(i.deck||'')}</p></a></article>`).join(''):'<p>Novas matérias estão entrando neste arquivo.</p>';}).catch(()=>root.innerHTML='<p>O arquivo está sendo atualizado.</p>');})();