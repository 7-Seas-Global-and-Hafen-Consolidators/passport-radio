(()=>{"use strict";let graph=null;const norm=s=>String(s||"").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().trim(),slug=s=>norm(s).replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"");async function load(){if(graph)return graph;const r=await fetch("/data/knowledge-graph.json");if(!r.ok)throw new Error("knowledge graph "+r.status);graph=await r.json();return graph}async function resolve(q){const g=await load(),n=norm(q);return(g.entities||[]).find(e=>norm(e.name)===n||(e.aliases||[]).some(a=>norm(a)===n))||null}async function related(id){const g=await load(),ids=new Set();(g.relations||[]).forEach(r=>{if(r.from===id)ids.add(r.to);if(r.to===id)ids.add(r.from)});return(g.entities||[]).filter(e=>ids.has(e.id))}window.PassportKnowledgeGraph={load,resolve,related,slug,norm}})();