# Identidade do usuário

Nome exibido: primeiro `profiles.display_name`; fallback `user_metadata.display_name`; fallback prefixo do e-mail. Isso preserva o comportamento que já reconheceu JOAN corretamente.
