#!/usr/bin/env node
import fs from 'fs';const feed=JSON.parse(fs.readFileSync('data/editorial-feed.json','utf8')),items=feed.items||[],staticPages=['','radio.html','loja.html','promocoes.html','promocao-uci-cinema.html','anuncie.html','editorial.html','destinos.html','radio-mundo.html','divulgar-bandas.html','minha-passport.html','contato.html','privacidade.html','termos.html','cookies.html'],now=new Date().toISOString();const urls=[...staticPages.map(p=>({loc:`https://passportradio.online/${p}`,lastmod:now,priority:p===''?'1.0':'0.7'})),...items.slice(0,500).map(i=>({loc:(i.url||'').startsWith('http')?i.url:'https://passportradio.online'+i.url,lastmod:new Date(i.published_at||Date.now()).toISOString(),priority:'0.6'}))];fs.writeFileSync('sitemap.xml',`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map(u=>`<url><loc>${u.loc}</loc><lastmod>${u.lastmod}</lastmod><changefreq>daily</changefreq><priority>${u.priority}</priority></url>`).join('')}</urlset>`);console.log('sitemap.xml',urls.length);
