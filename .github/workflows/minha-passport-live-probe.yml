name: Minha Passport Live Probe
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'minha-passport.html'
      - 'js/passport-auth.js'
jobs:
  live:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: sleep 45
      - run: node tests/minha-passport-live-probe.mjs
